def register(key, value):
  # Key not yet claimed
  if not self.storage[key]:
    self.storage[key] = value
    return(1)
  # Key already claimed
  else:
    return(0)

def ask(key):
  return(self.storage[key])
